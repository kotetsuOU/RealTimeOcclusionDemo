# 引継ぎ資料（刷り込みタスク設計・実装）

作成日：2026年9月16日
対象：東京大学 篠田研 M1 高久 和也
前提資料：`handoff.md`（2025年7月作成、研究の基本背景・先行文献の読み合わせ内容）を参照済みのうえで、本資料はその後の実験デザイン議論と実装作業をまとめたもの。

---

## 1. きっかけ：実験デザインの根本的な見直し

先輩との相談で、当初の「上からの刺激 vs 下からの刺激を比較させて弁別させる」デザインは、両者が物理的にはっきり区別可能なため、錯覚の有無ではなく単純な弁別能力を測ってしまう、という指摘があった。

もともとの研究の問い：**下から出したAUTD刺激を、上からの刺激だと錯覚させられるか**

この問いに立ち返り、デザインを再検討した。

---

## 2. 議論の流れと確定事項

### 2.1 反応バイアスの問題

下刺激のみを提示して「上/下どちらに感じたか」を問う設計だと、正誤の採点基準（ground truth）が存在せず、"上"と答えた率が「本当の錯覚」か「単なる反応バイアス・デマンド特性」か区別できない（信号検出理論でいう感度 d′ とバイアス c の混同）。

対策として、物理的な上/下刺激を両方混ぜて出す（catch trial方式）か、条件間比較でバイアスを差し引く方式が議論された。

### 2.2 問いの分離：存在検証 vs パラメータ探索

先輩案（周波数・変調タイプを自由調整して"最も上らしい"パラメータを探す）と、当初の問い（視覚情報で下刺激を上と錯覚させられるか）は、**問いの種類が異なる**ことが判明：

- **問い1（存在検証・confirmatory）**：視覚情報によって触覚定位が変化して感じるか → 対照条件（一致/不一致など）を用いた比較実験が必要
- **問い2（探索・exploratory）**：錯覚を起こしやすい刺激パラメータは何か → 調整法（method of adjustment）が向いている

**結論**：2段階の研究として設計する。

1. **実験1**：視覚キューの有無・一致/不一致による対照実験で、視覚による触覚定位シフトが統計的に起きるかを確認
2. **実験2**：実験1で効果が確認できたパラメータ範囲を前提に、周波数・変調・視覚キューの強さなどを調整法で探索し、最も効果的な設定を特定

調整法（自由調整）を使う場合のアンカリング対策：開始点のランダム化、上昇系列・下降系列の両方を実施して平均を取る。

### 2.3 刷り込み（priming）タスクのアイデア

ラバーハンド錯覚（Botvinick & Cohen, 1998）に倣い、視覚-触覚の同期刺激で錯覚を誘導してから、CCT（crossmodal congruency task）的な測定を行う案が浮上。

**設計案**：
- 視覚：手のひらの上をstick状のオブジェクト（cylinder）が一定間隔で移動し、触れる
- 触覚：それに同期して手の甲側にAUTD刺激を提示
- その後、これまで議論していたCCT（弁別課題＋視覚ディストラクター）を実施

**比較設計**：同期/非同期の比較ではなく、「刷り込みタスクによって錯覚が起きるか」を問いたいため、**刷り込みあり/なしのpre/post比較**を採用。学習効果と刷り込み効果の混同を避けるため、pre測定前に十分な練習ブロックを挟む対策が必要（統制群を置く案は工数の都合で保留）。

### 2.4 レビューした関連文献

| 文献 | 内容・関連性 |
|---|---|
| Pavani, Spence, & Driver (2000) *Psychological Science*, 11(5), 353–359 | 視覚ディストラクターによる触覚定位の乗っ取り（visual capture of touch）。上/下弁別課題＋一致/不一致ディストラクター。RT・エラー率両方に一致性効果（Exp1: 482ms/1.8% vs 574ms/11.3%）。ゴム手が本物の手と整列してる時のみ効果が増強。CCTパラダイムの直接の原型 |
| Spence, Pavani, & Driver (1998/2000) | Crossmodal Congruency Task (CCT) の原型。RT/正答率への干渉効果で視覚-触覚統合の強さを間接的に測定する手法論 |
| Botvinick & Cohen (1998) *Nature*, 391, 756 | ラバーハンド錯覚の原論文。同期ストローキングによる所有感・固有感覚ドリフトの誘導。刷り込み手続きのアンケート項目の元ネタ（錯覚特異的項目＋統制項目の構成） |
| Spence (2011) *Psychonomic Bulletin & Review* | クロスモーダル対応（ピッチ-高さなど）のレビュー。先輩案（周波数を振って上らしさを出す）の理論的背景として参照可能 |
| Christos, Mulholland, O'Leary, & White (2025) *i-Perception* | "Mismatched-palm" RHI。ラバーハンドの手のひら側を見ながら自分の手の甲側を触られても、所有感・視覚捕獲の錯覚は成立する。今回の刷り込みタスク（手のひら視覚／手の甲触覚）の設計を後押しする直接的な先行研究。ただしラバーハンドを使わない自分の手のみでの検証は先行研究になく、新規性のポイントになりうる |
| Moving Rubber Hand Illusion 系（Kalckert & Ehrsson 周辺） | 静的ストローキングでなく「動き」の同期による錯覚誘導。刷り込みタスクの「オブジェクトが移動する」という動的視覚刺激の設計根拠 |

### 2.5 Botvinick & Cohen型アンケートの構成（刷り込み効果測定に流用予定）

- **錯覚特異的項目**（例：「触覚が視覚刺激の位置から来てるように感じた」「見えている物体が自分の手のように感じた」）
- **統制項目**（デマンド特性チェック用。例：「視覚刺激が自分の手に似てきたように感じた」など、錯覚と直接関係ない誘導質問）

同期条件でのみ錯覚特異的項目が有意に上昇し、統制項目は動かない、というパターンを示せると説得力が増す。

---

## 3. Unity実装状況

### 3.1 全体方針

刷り込みタスクは「視覚側（cylinder移動）」と「触覚側（AUTD出力）」を、単一の時間軸（タイマー駆動）から同期させる設計。物理判定ベースの同期（Colliderのフレーム依存ジッター）は避け、視覚アニメーションと触覚出力の同期精度を優先。

### 3.2 座標系の確認事項

- 移動方向は奥行き＝**Z軸**（当初X軸と誤解していたが訂正済み）
- cylinderオブジェクトはX軸に30度回転させているが、これは見た目の傾きのみで移動方向とは無関係（`transform.position`はワールド座標系のため、回転の影響を受けない）と確認済み
- Y座標は既存システム（RealSense点群から取得）を流用し、必ず手のひらの上に乗るようにする予定（**未実装**、現状は`fixedY`のプレースホルダー）

### 3.3 完成済み：CylinderMover.cs（視覚側の移動ロジック）

```csharp
using UnityEngine;

public class CylinderMover : MonoBehaviour
{
    private enum CylinderState { Moving, Waiting }

    [Header("移動範囲")]
    [SerializeField] private float startZ = -1.0f;   // 手前側のZ座標
    [SerializeField] private float endZ = 1.0f;      // 奥側のZ座標
    [SerializeField] private float fixedX = 0.0f;    // X座標（固定値）
    [SerializeField] private float fixedY = 0.0f;    // Y座標（仮の固定値。後で点群取得に差し替え予定）

    [Header("タイミング")]
    [SerializeField] private float moveDuration = 2.0f;  // 手前→奥までの所要時間（秒）
    [SerializeField] private float waitDuration = 1.0f;  // 待機時間（秒）

    [Header("見た目の切り替え")]
    [SerializeField] private Renderer cylinderRenderer;  // インスペクターでCylinderのRendererをアタッチ

    private CylinderState state = CylinderState.Moving;
    private float elapsedTime = 0f;
    private float waitTimer = 0f;

    void Update()
    {
        switch (state)
        {
            case CylinderState.Moving:
                elapsedTime += Time.deltaTime;
                float t = Mathf.Clamp01(elapsedTime / moveDuration);

                float z = Mathf.Lerp(startZ, endZ, t);
                transform.position = new Vector3(fixedX, fixedY, z);

                cylinderRenderer.enabled = true;

                if (t >= 1f)
                {
                    state = CylinderState.Waiting;
                    waitTimer = 0f;
                    cylinderRenderer.enabled = false;
                }
                break;

            case CylinderState.Waiting:
                waitTimer += Time.deltaTime;
                if (waitTimer >= waitDuration)
                {
                    elapsedTime = 0f;
                    state = CylinderState.Moving;
                }
                break;
        }
    }
}
```

動作確認済みの想定挙動：手前(startZ)から出現 → 奥(endZ)へ移動して消える → waitDuration秒待機 → 手前から再出現、のループ。

### 3.4 未実装：触覚側（次のステップ）

以下は方針のみ確定、実装はこれから：

1. **接触検知**：`OnTriggerEnter`/`OnCollisionEnter`でcylinderと手のひらの接触を検知。`Collision`型なら`collision.GetContact(0).point`で接触点座標を取得
2. **オフセット適用**：接触点（手のひら側）から手の厚み分だけ法線方向にオフセットし、手の甲側の対応座標を算出（オフセット量は固定値で暫定運用、後でMediaPipeランドマークからの推定に差し替え検討）
3. **座標系変換**：`InverseTransformPoint()`でAUTDデバイスのローカル座標系に変換
4. **AUTD出力**：静止フォーカス用のGainクラス（`GainSTM`ではなく、静止点用の別クラス。AUTD3Sharp v38.0.1のAPI要確認）で送信。**接触が続いている間ずっと出力を継続**する方針（＝(a)接触開始時に1回だけ座標取得→固定位置で出力し続ける、を採用。接触点に追従して毎フレーム更新する(b)案は今回見送り。STM特有の125Hz×8点制約は今回のケースでは非該当）
5. **出力停止**：`OnTriggerExit`/`OnCollisionExit`で`StopOutput()`
6. **ログ**：接触検知タイムスタンプとAUTD出力送信タイムスタンプを両方CSVに記録（`Time.realtimeSinceStartupAsDouble`、既存の`CrossmodalTask.cs`のロギングパターンを流用予定）

検討事項として残っているが未確定：
- stickが手のひら表面付近で揺れて`Enter`/`Exit`が連続発火する可能性への対策（デバウンス処理）は、問題が出たら追加する方針で先送り中
- 手の厚みオフセットの具体的な決定方法（固定値 or MediaPipeランドマークからの推定）

---

## 4. 実装時の注意点まとめ（実装担当者向け）

- `Renderer.enabled`で見た目を切り替える（`GameObject.SetActive`は使わない。後者だと`Update()`自体が止まりタイマーが進まなくなるため）
- `transform.position`は常にワールド座標系。オブジェクトの`transform.rotation`とは独立して扱える
- Y座標の点群取得は生値だとノイズでガクつく可能性があるため、`Vector3.Lerp`や移動平均でのスムージングを推奨
- AUTD側の静止フォーカス出力は、既存の`MultiAUTD3Controller.cs`の`OutputSide`（Upper/Down/Both/None）による決め打ち出力とは別の、座標指定型の出力経路を新規に追加する必要がある
